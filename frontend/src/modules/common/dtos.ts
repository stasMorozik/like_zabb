export namespace Dtos {
  export type Email = {
    email: string
  }

  export type Password = {
    password: string
  }

  export type Name = {
    name: string
  }

  export type Message = {
    message: string
  }
}
